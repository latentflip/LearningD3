var pipeClient = require('../')

var client = pipeClient.createClient({app_id: 2, key: '256e0ddec0406ecc2eff', secret: 'bf377ab89f89c7ae3efd'})

client.on('connected', function() {
  client.channel('foo').trigger('a', 'b', function(err, data) {
    throw new Error();
  });
});

client.connect();